server:
  port: 8889

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/DileepVRN/TrinetraOmni.git
          default-label: master
          search-paths: config-service
          clone-on-start: true