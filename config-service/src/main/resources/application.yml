server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/DileepVRN/trinetra-config-repo
          default-label: main
          clone-on-start: true