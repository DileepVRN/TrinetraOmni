server:
  port: 8081

spring:
  application:
    name: user-service   # ✅ VERY IMPORTANT

  datasource:
    url: jdbc:mysql://localhost:3306/user_db
    username: root
    password: I@mcbg91778
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  sql:
    init:
      mode: always

# ✅ Eureka Client Config (MISSING before)
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

# Optional Logging
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE